# Build trees (all builds are out-of-tree, under build*/ at repo root)
build/
build-*/

# Editor / OS noise
.DS_Store
compile_commands.json
.cache/

# M4 corpus: pinned by scripts/fetch-linux-corpus.sh, too large for a submodule
third_party/linux/
